spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cursoSpringBoot
    username: postgres
    password: 7+7 post 7+7
    dbcp2:
      platform: postgres
      driverClassName: org.postgresql.Driver
  jpa:
    hibernate.ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show-sql: none
